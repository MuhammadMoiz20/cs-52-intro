// note: these are pretty basic, just smoke tests
const assert = require('assert');
const request = require('supertest');
// would normally start app here, skip for now

describe('notes', () => {
  it('placeholder', () => {
    assert.strictEqual(1 + 1, 2);
  });
});
